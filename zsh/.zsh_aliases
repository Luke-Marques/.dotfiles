# Better ls
alias ls="eza -a --long --icons always"
alias t="eza -a -T -L 2 --icons always"

# Better cd
alias cd="z"

# Better cat
alias cat="bat"

# Trash > rm
alias rm="trash"

# SSH aliases
alias create="ssh create"

# Call dotfiles setup script
alias setup-mac="bash ~/.dotfiles/setup.sh"
